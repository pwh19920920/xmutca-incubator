spring:
  application:
    name: xmutca-incubator-passport
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server_addr: localhost:8848
        file-extension: yml
