<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.xmutca.incubator.member.repository.UserInfoRepository">
    <resultMap id="resultMap" type="com.xmutca.incubator.member.model.UserInfo">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
    </resultMap>

    <sql id="columns">
             id, username, password, salt
        </sql>

    <select id="getByUsername" resultMap="resultMap">
        select <include refid="columns"/>
        from member_user_info
        where username = #{username}
    </select>
</mapper>
