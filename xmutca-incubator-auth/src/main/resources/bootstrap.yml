spring:
  application:
    name: xmutca-incubator-auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server_addr: localhost:8848
        file-extension: yml
