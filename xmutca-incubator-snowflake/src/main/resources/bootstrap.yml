spring:
  application:
    name: xmutca-incubator-snowflake
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server_addr: localhost:8848
        file-extension: yml
        shared-dataids: application.yml
        refreshable-dataids: application.yml