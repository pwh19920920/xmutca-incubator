spring:
  application:
    name: xmutca-incubator-example-provider
  cloud:
    nacos:
      discovery:
        server_addr: localhost:8848
      config:
        server_addr: localhost:8848
        file_extension: yml