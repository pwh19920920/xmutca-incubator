spring:
  application:
    name: xmutca-incubator-example-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8888
      config:
        serverAddr: localhost:8888
        file-extension: yml
